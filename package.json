{
  "private": true,
  "name": "yw-monorepo",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "newArticle": "yarn workspace cli build",
    "dev": "yarn workspace @yw/web dev",
    "build": "yarn workspace @yw/web build"

  }
}
